GEMINI_API_KEY=
# The free tier has a per-model daily quota (20 requests/day).
# If you hit "quota exceeded", switch to another model your key can access
# (each model has its own quota bucket), e.g. gemini-3.5-flash.
GEMINI_MODEL=gemini-3.5-flash
PORT=3001