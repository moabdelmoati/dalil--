# Gemini API key (Required for serverless backend /api endpoints)
GEMINI_API_KEY=your_gemini_api_key_here

# Optional: Override Gemini Model (Default is gemini-2.5-flash)
GEMINI_MODEL=gemini-2.5-flash

# Optional for frontend client (leave empty for same-origin serverless /api)
VITE_API_URL=
